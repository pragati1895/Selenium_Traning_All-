package com.cg.project;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.Select;
/*
public class Test {

public void Language() {
	WebElement lan=driver.findElement(By.cssSelector("#msdd"));
	Select s=new Select(lan);
	s.selectByIndex(1);
	//System.out.println(lan.getText());
}
public void Skill() {
	WebElement skillfield=driver.findElement(By.xpath("//*[@id=\"Skills\"]"));
	Select s =new Select(skillfield);
	s.selectByIndex(2);
	//System.out.println(skillfield.getText());
}
public void Country() {
WebElement country1=(WebElement) driver.findElements(By.xpath("//*[@id=\"basicBootstrapForm\"]/div[10]/div/span/span[1]/span"));
 Select c=new Select(country1);
 c.selectByValue("2");
	 
	 
}
public void DOBallSection() {
	 List<WebElement>Year=driver.findElements(By.xpath("//*[@id=\"yearbox\"]"));
	 Select s=new Select((WebElement) Year);
	 s.selectByValue("1995");
	  WebElement Month=driver.findElement(By.xpath("//*[@id=\"basicBootstrapForm\"]/div[11]/div[2]/select"));
	  Select s1=new Select(Month);
	  s1.deselectByIndex(8);
	   WebElement day=driver.findElement(By.xpath("//*[@id=\"daybox\"]"));
	   Select s2= new Select(day);
}

public void password() {
	WebElement Rpass=driver.findElement(By.id("firstpassword"));
	Rpass.sendKeys("pL#hipl@2023");
}
public void Cpass() {
	WebElement Spass=driver.findElement(By.id("secondpassword"));
}

public void SubmitButton() {
	 WebElement sbutton=driver.findElement(By.id("submitbtn"));
	 sbutton.click();
}
}

}
*/